@model IEnumerable<PatientCareSubsystem.Data.Models.Domain.SisterNurse>

@{
    ViewData["Title"] = "Sister Nurses";
}

<h2>@ViewData["Title"]</h2>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Phone Number</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var sisterNurse in Model)
        {
            <tr>
                <td>@sisterNurse.Id</td>
                <td>@sisterNurse.FirstName</td>
                <td>@sisterNurse.LastName</td>
                <td>@sisterNurse.PhoneNumber</td>
                <td>@sisterNurse.Email</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@sisterNurse.Id" class="btn btn-warning btn-sm">Edit</a>
                    <a asp-action="GetById" asp-route-id="@sisterNurse.Id" class="btn btn-info btn-sm">View</a>
                    <a asp-action="Delete" asp-route-id="@sisterNurse.Id" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Add" class="btn btn-primary">Add New Sister Nurse</a>
